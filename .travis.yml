language: node_js

os: linux

node_js:
  - 8

script:
  - yarn run lint
  - yarn run test
  - yarn run build

notifications:
  email:
    on_failure: change

install:
- yarn

cache:
  yarn: true
  directories:
    - node_modules
